const nome='Gabriel';
const nome1="Trem";
const num1=10;
const num2=10.52;
let nomeAluno; //undefined -> não aponta paralocal nenhum na memória
let sobrenomeAluno = null; //Nulo -> não apota para local nenhum na memória
const aprovado =  false; //Boolean = true, false (lógico)

console.log(typeof aprovado,"\n",num2);